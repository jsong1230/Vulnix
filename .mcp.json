{
  "mcpServers": {
    "database": {
      "command": "npx",
      "args": ["-y", "${DB_MCP_PACKAGE:-@anthropic-ai/mcp-server-postgres}", "${DATABASE_URL}"],
      "description": "DB 스키마 조회 + 읽기 전용 쿼리"
    },
    "chrome-devtools": {
      "command": "npx",
      "args": ["-y", "chrome-devtools-mcp@latest"],
      "description": "Chrome DevTools: 스크린샷 캡처, 브라우저 자동화, 콘솔/네트워크/DOM 검사, 성능 분석"
    }
  }
}
