rules:
  - id: vulnix.go.crypto.hardcoded_key
    pattern-either:
      - patterns:
          - pattern: |
              $KEY := "..."
              jwt.NewWithClaims(..., $KEY)
      - pattern: "[]byte(\"...\")"
    message: "하드코딩된 시크릿 키."
    languages: [go]
    severity: WARNING
    metadata:
      cwe: CWE-798
      owasp: A02:2021 - Cryptographic Failures
      category: hardcoded_credentials
