# ============================================================
# Vulnix Backend 환경변수 템플릿
# 이 파일을 복사하여 .env 파일을 생성하고 실제 값을 입력하세요.
# cp .env.example .env
# ⚠️  .env 파일은 절대 Git에 커밋하지 마세요.
# ============================================================

# ---- Database ----
# PostgreSQL 연결 URL (asyncpg 드라이버 사용)
DATABASE_URL=postgresql+asyncpg://user:pass@localhost:5432/vulnix

# ---- Redis ----
# Redis 연결 URL (RQ 워커 큐)
REDIS_URL=redis://localhost:6379

# ---- GitHub App ----
# GitHub App ID (GitHub App 설정 페이지에서 확인)
GITHUB_APP_ID=123456

# GitHub App Private Key (PEM 형식, 줄바꿈은 \n으로 이스케이프)
GITHUB_APP_PRIVATE_KEY="-----BEGIN RSA PRIVATE KEY-----\nYOUR_PRIVATE_KEY_HERE\n-----END RSA PRIVATE KEY-----"

# GitHub Webhook 서명 검증 시크릿
GITHUB_WEBHOOK_SECRET=your-webhook-secret

# GitHub OAuth App Client ID
GITHUB_CLIENT_ID=Iv1.xxxxxxxxxxxxxxxx

# GitHub OAuth App Client Secret
GITHUB_CLIENT_SECRET=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ---- Claude API ----
# Anthropic API 키 (https://console.anthropic.com)
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ---- JWT 설정 ----
# JWT 서명 비밀키 (openssl rand -hex 32 로 생성 권장)
JWT_SECRET_KEY=your-super-secret-jwt-key-change-this-in-production

# JWT 알고리즘
JWT_ALGORITHM=HS256

# Access Token 만료 시간 (분)
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30

# Refresh Token 만료 시간 (일)
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# ---- CORS 설정 ----
# 쉼표로 구분된 허용 오리진 목록
CORS_ORIGINS=http://localhost:3000,https://vulnix.vercel.app

# ---- 앱 설정 ----
# 환경 (development / staging / production)
APP_ENV=development

# 앱 이름
APP_NAME=Vulnix

# 디버그 모드 (production에서는 반드시 false)
DEBUG=true
